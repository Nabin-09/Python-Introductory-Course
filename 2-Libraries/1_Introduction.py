"""Definition of a Module
A Python module is essentially a file containing Python code, which can include definitions for functions, classes, and variables. It may also contain executable code. The primary purpose of modules is to organize code logically, making it easier to manage and reuse across different programs
import keyword is used to import contents of a module   
"""
import random

s = random.choice(["tails","heads"])
print(s)